# Cloudflare Turnstile site key (free CAPTCHA).
# Create one at https://dash.cloudflare.com/?to=/:account/turnstile
# If unset, the app falls back to Cloudflare's "always passes" test key.
VITE_TURNSTILE_SITE_KEY=
